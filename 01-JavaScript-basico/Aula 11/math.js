 let num = 9.532334

//let numero = Math.floor(num) //Arredonda o número para o valor inteiro mais baixo
//let numero = Math.ceil(num) //Arredonda o número para o valor inteiro mais alto
//let numero = Math.round(num) //Arredonda o numero para o valor inteiro mais perto
//let numero = Math.max(1,3,56,6,12,33) //Retorna o valor máximo digitado
//let numero = Math.min(30,32,5,1,-12)//Retorna o valor minimo digitado
//let numero = Math.sqrt(num) //Retorna a raiz quadrada, a raiz quadrada tbm pode ser calculada com (num ** 0.5)
//Teste de sorteio

const aleatorio = Math.round(Math.random() * ( 100 - 1 ) + 1) //Gerar um número aletorio entre 100 e 1 
console.log(aleatorio)

console.log(Math.log(3))